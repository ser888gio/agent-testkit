## What and why

## How was this tested

- [ ] `python -m pytest` (full suite) or the narrowest relevant subset
- [ ] `bash tools/validate.sh` (or `--affected` while iterating)

## Checklist

- [ ] Tests added/updated for behavior changes
- [ ] `docs/specs/` updated if a public contract changed (schema, config, assertion registry)
- [ ] No unrelated refactors bundled in
